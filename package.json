{
  "name": "tree-house-emails",
  "version": "1.0.0",
  "description": "Tree House email module",
  "main": "build/index.js",
  "types": "build/index.d.ts",
  "scripts": {
    "clean": "rm -rf ./build/*",
    "build": "npm run clean && tsc",
    "lint": "tslint 'src/**/*.ts'",
    "test": "jest",
    "test:coverage": "jest --coverage --collectCoverageFrom=src/**/*.ts",
    "test:watch": "jest --watchAll",
    "pretest": "npm run build",
    "prepare": "npm run build",
    "release": "node_modules/np/source/cli.js --no-cleanup"
  },
  "precommit": [
    "lint"
  ],
  "dependencies": {
    "mandrill-api": "^1.0.45"
  },
  "devDependencies": {
    "@types/jest": "^23.3.13",
    "@types/mandrill-api": "^1.0.30",
    "coveralls": "^3.0.2",
    "jest": "^24.0.0",
    "node-mocks-http": "^1.7.3",
    "np": "^4.0.2",
    "pre-commit": "^1.2.2",
    "supertest": "^3.4.2",
    "ts-jest": "^23.10.5",
    "tslint": "^5.12.1",
    "tslint-config-airbnb": "^5.11.1",
    "typescript": "^3.2.4"
  },
  "engines": {
    "node": ">=6.0.0"
  },
  "directories": {
    "test": "test"
  },
  "keywords": [
    "tree House",
    "treehouse",
    "treehouse emails",
    "treehouse emails"
  ],
  "license": "ISC",
  "repository": "git@github.com/icapps/tree-house-emails.git",
  "bugs": {
    "url": "https://github.com/icapps/tree-house-emails/issues"
  },
  "homepage": "https://github.com/icapps/tree-house-emails/issues#readme"
}
